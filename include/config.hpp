#pragma once

// Refresh interval (milliseconds)
constexpr int REFRESH_INTERVAL_MS = 250;

// UI layout
constexpr float SEC1_HEIGHT_RATIO = 0.25f;

// Colors (ncurses color IDs)
constexpr short COLOR_HEADER = 1;
constexpr short COLOR_HIGHLIGHT = 2;

// Limits
constexpr int MAX_COMMAND_LEN = 64;
